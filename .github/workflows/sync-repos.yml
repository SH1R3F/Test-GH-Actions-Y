name: attmpt 123
on: push
permissions:
  contents: write
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: pulling from other repo
        shell: bash
        run: |
          git config --global user.email "Bot@example.com"
          git config --global user.name "Bot"
          git clone https://github.com/SH1R3F/Test-GH-Actions-Y.git
          cd Test-GH-Actions-Y
          git remote remove origin
          git remote add origin https://github.com/SH1R3F/Test-GH-Actions-X.git
          git add .
          git status
          echo "~~~~~~~~~~~~~~~~"
          ls
          git commit -m "test"
          git push -f
